# RAG Local Data

Put local knowledge-base PDFs here. This directory is ignored by git except for
this README.

The first RAG implementation extracts readable text from PDFs and builds a
temporary in-memory retrieval index per query.

Do not commit customer, production, or private documents.
