# Text2SQL Local Data

Put local Text2SQL CSV files here. This directory is ignored by git except for
this README.

Default filenames used by `subagents/text2sql/AGENT.yaml`:

- `resources.csv` -> table `resources`
- `sea_cable_faults.csv` -> table `sea_cable_faults`

Do not commit customer, production, or private datasets.
